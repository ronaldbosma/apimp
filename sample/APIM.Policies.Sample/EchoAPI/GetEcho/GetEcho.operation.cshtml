<policies>
    <inbound>
        <base />
    </inbound>

    <backend>
        <base />
    </backend>

    <outbound>
        <base />
        <choose>
            <when condition="@method:APIM.Policies.Sample.EchoAPI.GetEcho.GetEchoExpressions.ResponseIsError">
                <set-body>Error</set-body>
            </when>
            <otherwise>
                <set-body>@method:APIM.Policies.Sample.EchoAPI.GetEcho.GetEchoExpressions.TransformResponse</set-body>
            </otherwise>
        </choose>
    </outbound>
    
    <on-error>
        <base />
    </on-error>
</policies>